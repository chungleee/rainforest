<h1><%= @product.name %></h1>

<div class="description-box">
  <%= @product.description %>
</div>
<br>
<%= link_to "Edit product", edit_product_path %>
<%= link_to "Delete product", product_path(@product), method: :delete, data: { confirm: "You sure fam?"} %>
